// program in output name and adress
#include <stdio.h>

int main(void) {  // можно войд захуярить, чтобы наверняка ничего в функцию не
                  // подавать
  int home;
  home = 12;
  printf("Galiev timurlan, Mochishenskoe shosse %d\n", home);
  return 0;
}